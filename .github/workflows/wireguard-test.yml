name: Wireguard CI
on:
  pull_request:
    branches:
      - main
    paths:
      - actions/wireguard/*
  push: 
    branches:
      - main
    paths:
      - actions/wireguard/*
jobs:
  test: 
    runs-on: 'ubuntu-latest'
    steps:
    - uses: actions/checkout@v2
    - uses: ./actions/wireguard
